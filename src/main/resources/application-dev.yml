#-- Spring Boot Configuration
spring:
  profiles:
    active: "dev"
  data:
    mongodb:
      host: localhost
      port: 27017
      database: crm
  freemarker:
    template-loader-path: classpath:/
      
server:
  port: 8080
  context-path: /
   
#-- Application Configuration
app:
  image:
    path: C:/images/
    size: 8000000
    maxWidth: 1200
    maxHeight: 1200

      
  #Application    
  url: localhost:8080
